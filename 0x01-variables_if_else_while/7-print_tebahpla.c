#include <stdio.h>

/**
 * main - Prints the lowercase alphabet in reverse.
 *
 * Return: 0
 */

int main(void)
{
	char x;

	for (x = 'z'; letter >= 'a'; letter--)
		putchar(x);

	putchar('\n');

	return (0);
}
